# 2026 年主流開源 LLM 架構圖鑑

把 gemma-4、Qwen 3.6/3.8、DeepSeek-V4、Kimi K3、MiniMax M2 的設定檔攤開放在一起，會看到一個共同的問題與四種不同的答案：**注意力對序列長度是平方成本，要支援十萬到百萬 token 的 context，就必須讓大部分的層不做全域注意力。**

誰在哪些層做全域注意力、剩下的層用什麼替代、以及 MoE 的稀疏度推到多高——這三個決定就構成了 2026 年這批模型的架構差異。

所有數字取自各模型 HuggingFace repo 的 `config.json` 與官方模型卡，查詢日 **2026-08-18**。

- 相關：[vLLM 架構與 KV cache](vllm-serving-and-architecture.md)、[推論速度怎麼估](llm-decode-throughput-formula.md)、[量化格式](quantization-fp8-nvfp4.md)

---

## 一覽表

| 模型 | 層數 | hidden | 總參數 | 激活參數 | 注意力組成 | 專家 / top-k | context |
|---|---:|---:|---:|---:|---|---|---:|
| gemma-4-E4B-it | 42 | 2560 | 8.0 B | 約 4 B | 滑動512 ×35 / 全域 ×7 + KV 共享 18 層 | dense | 128K |
| gemma-4-26B-A4B-it | 30 | 2816 | 26.5 B | 4 B | 滑動1024 ×25 / 全域 ×5 | 128 / 8 | 256K |
| Qwen3.6-27B | 64 | 5120 | 27.8 B | 27.8 B | 線性 ×48 / 全域 ×16 | dense | 256K |
| Qwen3.6-35B-A3B | 40 | 2048 | 36.0 B | 3 B | 線性 ×30 / 全域 ×10 | 256 / 8 + 1 shared | 256K |
| Qwen3.8-27B | 64 | 5120 | 27.8 B | 27.8 B | 線性 ×48 / 全域 ×16 | dense | 256K |
| Qwen3.8-2.4T-A95B | 92 | 8192 | 2446 B | 95 B | 線性 ×69 / 全域 ×23 | 512 / 10 + 1 shared | 256K |
| DeepSeek-V4-Flash | 43 | 4096 | 290.9 B | 13 B | MLA + 滑動128 | 256 / 6 + 1 shared | **1M** |
| DeepSeek-V4-Pro | 61 | 7168 | 1598.8 B | 49 B | MLA + 滑動128 | 384 / 6 + 1 shared | **1M** |
| Kimi-K3 | 93 | 7168 | 2779.9 B | 104 B | KDA ×69 / Gated MLA ×24 | **896 / 16** + 2 shared | **1M** |
| MiniMax-M2.7 | 62 | 3072 | 228.7 B | 約 10 B | **全部全域注意力** | 256 / 8 | 200K |

「激活參數」有官方數字的用官方（型號名稱裡的 `A4B`、`A3B`、`A95B` 就是），沒有的標「約」，為依 config 計算的估值。

---

## 四種長 context 策略

### 策略一：滑動視窗交錯（gemma-4）

大部分層只看最近 N 個 token，少數層做全域注意力。

```
gemma-4-E4B-it   42 層 · hidden 2560 · 8.0 B
┌────────────────────────────────────────────────────────────────┐
│ 層 0-4    滑動注意力 (window 512)  ┐                            │
│ 層 5      全域注意力               ├─ 這 6 層一組，重複 7 次     │  自有 KV
│ 層 6-10   滑動注意力               │                            │  (0-23 層)
│ 層 11     全域注意力               ┘                            │
│  ⋮                                                              │
│ ══════════ 層 24 之後：num_kv_shared_layers = 18 ══════════      │
│ 層 24-41  注意力型別相同，但 K/V 讀前面同型別層算好的           │  無自有 KV
└────────────────────────────────────────────────────────────────┘
KV 成本：42 層裡只有 24 層維護 KV，其中僅 4 層是全域注意力
```

`gemma-4-26B-A4B-it` 用同樣的 5:1 節奏（滑動視窗放寬到 1024），但沒有 KV 共享，並把 MLP 換成 128 選 8 的 MoE。

這個策略的實測效果很好：E4B 每個 token 的 KV 成本只有 22.3 KiB，遠低於「42 層全域」該有的 86 KB（實測數字見 [vLLM 架構與 KV cache](vllm-serving-and-architecture.md)）。

### 策略二：線性注意力混合（Qwen 3.5+、Kimi K3）

把大部分層換成**線性注意力**——它對序列長度是線性而非平方成本，代價是表達力較弱，所以每隔幾層插一層真正的全域注意力來補。

```
Qwen3.8-2.4T-A95B   92 層 · hidden 8192 · 2.45 T
┌────────────────────────────────────────────────────────────────┐
│ 層 0-2    線性注意力  ┐                                         │
│ 層 3      全域注意力  ├─ 3:1 節奏，重複 23 次                    │
│ 層 4-6    線性注意力  │                                         │
│ 層 7      全域注意力  ┘                                         │
│  ⋮                                                              │
│ 每層 FFN：512 個專家選 10 個 (moe_int 2048) + 1 個 shared        │
└────────────────────────────────────────────────────────────────┘
線性 69 層 : 全域 23 層  →  只有 25% 的層有平方成本
```

`Qwen3.6-27B` 與 `Qwen3.8-27B` 的 config 逐項相同（64 層、hidden 5120、線性 48 : 全域 16、27.78 B），差別在權重而非架構——同一個架構家族的兩次訓練。

Kimi K3 走的是同一條路但更激進：

```
Kimi-K3   93 層 · hidden 7168 · 2.8 T 總 / 104 B 激活
┌────────────────────────────────────────────────────────────────┐
│ 層 1      Dense FFN（唯一一層 dense）                            │
│ 層 1-3    KDA (Kimi Delta Attention，線性)  ┐                   │
│ 層 4      Gated MLA（全域，KV 壓縮）        ├─ 每 4 層一次全域   │
│ 層 5-7    KDA                               │                   │
│ 層 8      Gated MLA                         ┘                   │
│  ⋮  全域層固定在 4, 8, 12, ... 92, 93                            │
│ 每層 FFN：896 個專家選 16 個 + 2 個 shared（Stable LatentMoE）   │
│ 精度：MXFP4 權重 / MXFP8 啟動值，量化感知訓練                    │
└────────────────────────────────────────────────────────────────┘
KDA 69 層 : Gated MLA 24 層
```

Kimi K3 有兩點特別值得記：**896 個專家選 16 個**是目前公開模型裡稀疏度最高的配置之一（1.8% 激活率）；而且它從 SFT 階段就做**量化感知訓練**，權重原生就是 MXFP4——不是事後量化的產物。這讓「量化會掉多少品質」對它而言是個不太一樣的問題（見 [量化與品質](quantization-quality-and-offload.md)）。

### 策略三：壓縮 KV + 極小滑動視窗（DeepSeek-V4）

不換注意力機制，而是把 KV 本身壓小。MLA（Multi-head Latent Attention）把 K/V 投影到一個低維潛在空間再存，配上只有 128 的滑動視窗。

```
DeepSeek-V4-Flash   43 層 · hidden 4096 · 290.9 B 總 / 13 B 激活
┌────────────────────────────────────────────────────────────────┐
│ 每層注意力：MLA                                                  │
│   q_lora_rank 1024  →  Q 也走低秩投影                            │
│   num_key_value_heads = 1  →  KV 壓到單一 head                   │
│   qk_rope_head_dim 64      →  只有一小部分維度帶位置編碼         │
│   sliding_window 128       →  局部視窗極小                       │
│                                                                 │
│ 每層 FFN：256 個專家選 6 個 (moe_int 2048) + 1 個 shared         │
│                                                                 │
│ context 1,048,576 token                                         │
└────────────────────────────────────────────────────────────────┘
Pro 版同構放大：61 層 / hidden 7168 / 128 heads / 384 專家 / 49 B 激活
```

`num_key_value_heads = 1` 是這個設計最極端的地方——一般 GQA 會留 4 到 8 個 KV head，MLA 直接壓到 1，靠低秩潛在表示補回表達力。這是它能撐到 1M context 的主因。

### 策略四：不做特殊處理（MiniMax-M2.7）

```
MiniMax-M2.7   62 層 · hidden 3072 · 228.7 B 總 / 約 10 B 激活
┌────────────────────────────────────────────────────────────────┐
│ 每層注意力：全域（attn_type_list 全部是同一型別，無滑動/線性）    │
│   48 heads × head_dim 128，8 個 KV head（標準 GQA）              │
│ 每層 FFN：256 個專家選 8 個 (moe_int 1536)，無 shared expert     │
│ context 204,800，rope_theta 5,000,000                           │
└────────────────────────────────────────────────────────────────┘
```

在這批模型裡是個對照組：**它不用任何注意力稀疏化技巧**，全部 62 層都做完整的全域注意力，靠標準 GQA（8 個 KV head）控制 KV 大小，把稀疏化的預算全部押在 MoE 上。代價是 context 只到 200K，明顯低於同期其他模型的 256K 到 1M。

---

## MoE 的演化方向

把幾個 MoE 模型的專家配置排在一起，趨勢很明顯：

| 模型 | 專家數 | top-k | 激活率 | 單一專家的 intermediate | shared |
|---|---:|---:|---:|---:|---:|
| gemma-4-26B-A4B | 128 | 8 | 6.3% | 704 | 0 |
| DeepSeek-V4-Flash | 256 | 6 | 2.3% | 2048 | 1 |
| MiniMax-M2.7 | 256 | 8 | 3.1% | 1536 | 0 |
| Qwen3.6-35B-A3B | 256 | 8 | 3.1% | 512 | 1 |
| DeepSeek-V4-Pro | 384 | 6 | 1.6% | 3072 | 1 |
| Qwen3.8-2.4T-A95B | 512 | 10 | 2.0% | 2048 | 1 |
| **Kimi-K3** | **896** | **16** | **1.8%** | 3072 | 2 |

三個方向：

**專家數上升、激活率下降。** 從 128 個專家 6.3% 激活，走到 896 個專家 1.8% 激活。這是在同樣的推論成本下裝進更多知識——總參數可以推到兆級，而每個 token 的計算量維持在幾十 B。

**shared expert 成為標配。** DeepSeek、Qwen、Kimi 都有一到兩個「每個 token 都會經過」的共享專家，用來承載通用能力，讓 routed experts 專注在分化。gemma-4 與 MiniMax 沒有。

**top-k 大多維持在 6–8，Kimi 是例外。** 選 16 個專家配 896 個總數，是把「細粒度專家」推得最遠的——每個專家更小更專門，靠選更多個來組合。

---

## 對部署的意義

**KV cache 的成本差異極大，而它決定併發。** 同樣宣稱支援長 context，滑動視窗、線性注意力、MLA 三種做法的 KV 佔用可以差好幾倍。規劃容量時不能只看 context 上限，要看 config 裡的 `layer_types`、`num_key_value_heads` 與 `sliding_window`。

**激活參數決定速度，總參數決定顯存。** 這兩個數字在 MoE 上分離得很開——Kimi K3 的 2.8 T 總參數需要極大的記憶體，但 104 B 的激活量讓它的生成速度接近一個百 B 級 dense 模型。估算方法見 [推論速度怎麼估](llm-decode-throughput-formula.md)。

**線性注意力層的推論支援不如全域注意力成熟。** Qwen 3.5+ 與 Kimi 的線性層需要推論引擎專門實作，不是所有框架都跟上。選型時要確認目標引擎的版本支援度。

**原生量化的模型不要再量化一次。** Kimi K3 已經是 MXFP4 的量化感知訓練產物，再套一層事後量化沒有意義，也不會有預期的體積收益。

---

## 附註：MiniMax-H3 不是語言模型

`MiniMaxAI/MiniMax-H3` 是**影片與音訊生成模型**（tags 含 `text-to-video`、`image-to-video`、`text-to-audio-video`，基於 `diffusers`），33.1 B 參數，不屬於本文比較的 LLM。MiniMax 的語言模型是 M 系列，目前最新的是 `MiniMax-M2.7`。

同名混淆在這一代模型上很常見，查 config 之前先確認 `pipeline_tag` 與 `library_name` 可以省下不少時間。
